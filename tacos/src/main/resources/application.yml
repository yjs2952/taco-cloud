
taco:
  orders:
    pageSize: 10
spring:
  thymeleaf:
    cache: false
  data:
    rest:
      base-path: /api

logging:
  path: /var/logs/
  file: TacoCloud.log
  level:
    root: warn
    tacos: debug
    org.springframework.security: debug